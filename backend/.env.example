# Google Maps Distance Matrix (server-side only)
GOOGLE_MAPS_API_KEY=

# Comma-separated IPs allowed to call this function (your public IP(s))
ALLOWED_CALLER_IPS=

# Firebase project (defaults to Application Default Credentials on Cloud Functions)
FIREBASE_PROJECT_ID=route-optimizer-11
