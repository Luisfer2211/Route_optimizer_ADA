# Copy to env.deploy.yaml (gitignored) and fill in your values.
# Used by deploy.ps1 / deploy.sh and update-env.ps1 (no full redeploy needed for IP changes).
#
# YAML tips:
# - Always quote ALLOWED_CALLER_IPS (commas and IPv6 colons confuse YAML otherwise).
# - Add IPs from the function health URL "clientIp" field (phone + home + school).

FIREBASE_PROJECT_ID: route-optimizer-11

ALLOWED_CALLER_IPS: "181.189.154.189,200.119.172.237"

GOOGLE_MAPS_API_KEY: "YOUR_SERVER_MAPS_API_KEY"
